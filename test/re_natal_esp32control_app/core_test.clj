(ns re-natal-esp32control-app.core-test
  (:require [clojure.test :refer :all]
            [re-natal-esp32control-app.core :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
